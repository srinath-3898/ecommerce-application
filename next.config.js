/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["https:/prasadyash2411.github.io/ecom-website/img"],
    unoptimized: true,
  },
};

module.exports = nextConfig;
